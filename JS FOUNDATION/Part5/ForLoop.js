// 1st

let MultipliedNumbers = []
let numbers = [2, 3, 6]

for (let A = 0; A < numbers.length; A++) {
  let takenum = numbers[A] * 2;
  MultipliedNumbers.push(takenum)

}
// console.log(MultipliedNumbers);

//  2nd
 let cities = ["Patna", "Muzffarpur", "Bhaglpur", "Motihari"];
 let city = [];

 for(let L = 0; L < cities.length; L++){
  city.push(cities[L]);

 }
  console.log(city);
